# Stability release — 24 September 2026

Avi explicitly authorized completion and deployment in this task. Release is based on production commit 9728035, in an isolated checkout; unrelated calibration, research, memory commits and operational scripts are excluded.

## Changes
- Fix portfolio initial-load loop, full/unavailable slot controls and stock-card overlap.
- Surface history, analytics, dreams, signals, scan, futures and System request failures; bound JSON body and scan-stream requests; never automatically replay writes.
- Harden paper execution input/safety gates, small-position exit allocations, risk-based sizing and atomic JSON writes.
- Respect configured JSON storage mode in the existing Supabase health probe.
- Make Airtable health checks read-only and cache results; honor bounded rate-limit cooldowns. No strategy threshold or execution-mode change.

## Verification before release
- 29 frontend regression tests passed.
- 17 backend tests plus 25 subtests passed (execution, storage, sizing, health adapters).
- Production frontend build passed. Browser compatibility-data warning remains.
- Read-only predeployment backups of paper portfolio, signals and printing were saved outside this commit. All contained zero open/closed positions; main portfolio cash was 25000 and executor mode was paper.
- Backend /health identified JSON storage mode. The free Render instance does not establish durable disk persistence. No storage migration is claimed.

## Operational limits and rollback
Telegram's deployed credential returned 401; a local credential was valid. Owner credential update was requested through Render; no token is included in this release. Airtable quota may still require account action even after eliminating health-check writes. Model decisions, brokerage execution and profitability are not certified by UI regression tests.

Frontend and backend auto-deploy from main. Confirm both deployed commit IDs, live homepage slot controls and tab loading, then backend health and monitor status. Roll back to the previously deployed 9728035 artifact if release regression is observed; preserve runtime data. Never reset the unrelated dirty working tree.
